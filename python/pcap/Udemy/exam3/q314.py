#!/usr/bin/python3
# You have installed the pandas package on your Python development environment some time ago.

# You would like to update your current version of this package on your environment using pip.

# Which command will perform the needed update ?

# Note : pip is not explicitly listed in the PCAP-31-03 syllabus but some questions in the PCAP-31-03 exam may refer to it.

# pip is a utility (console tool) to install Python packages. Here are the few commands to install, update or un-install a package:

# --> pip install pack_name   will install the package "pack_name" (for all users).

# --> pip install -U pack_name   will update the previously installed package named "pack_name".

# --> pip uninstall pack_name   will uninstall the package "pack_name"