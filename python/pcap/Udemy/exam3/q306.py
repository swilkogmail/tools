#!/usr/bin/python3
# When a module is first imported, Python deploys the pyc files in a directory called :

# Explanation:

# During the very first import of a module, Python translates its source code into the semi-compiled format stored inside the pyc 
# files, and deploys these files into the __pycache__ directory located in the module's home directory.